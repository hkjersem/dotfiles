{
  "skill_name": "repo-audit",
  "evals": [
    {
      "id": 1,
      "prompt": "Audit this Python repository snapshot for correctness risks. Work only from the supplied evidence; do not inspect your current checkout, run code, access the network, or change files. Limit the result to at most three findings, not three required findings.\n\nREADME.md:1: Ledgerkit is a Python library that totals integer amounts in complete batches. Every amount in a batch must contribute to its total.\npyproject.toml:1-3: Python >=3.11; no JavaScript tooling is used.\nsrc/ledgerkit/totals.py:\n1: def batch_total(amounts: list[int]) -> int:\n2:     total = 0\n3:     for amount in amounts[:-1]:\n4:         total += amount\n5:     return total\n\ntests/test_totals.py:\n1: from ledgerkit.totals import batch_total\n2: def test_empty_batch():\n3:     assert batch_total([]) == 0\n\nA local triage report also flags generated/protocol.py at 950 lines with high churn and a test-file/source-file ratio of 1:12. The generated protocol file is not supplied. No other source, configuration, history, or command output is available.",
      "expected_output": "One supported correctness finding at src/ledgerkit/totals.py:3: slicing excludes the final amount, violating the stated batch-total contract. Concrete examples are labeled as reasoning, not executed tests, and a focused regression case and iteration fix are suggested. Generated-code size, churn, and file-count ratios are not asserted as independent defects. The report names coverage limits and remains stack-neutral and read-only.",
      "files": [],
      "assertions": [
        "Identifies exclusion of the final amount at src/ledgerkit/totals.py:3 and connects it to incorrect batch totals.",
        "Gives a concrete next action addressing the slice and missing nonempty-batch regression coverage.",
        "Groups the missing regression coverage with the slice defect rather than counting the same failure mechanism and remedy as separate findings.",
        "Does not promote generated-file size, churn, or test-file ratios into unsupported defects or pad the finding limit.",
        "Distinguishes code-based inference from executed results and states the snapshot's coverage limits.",
        "Does not modify files, run repository code, use the network, or assume JavaScript tooling."
      ]
    },
    {
      "id": 2,
      "prompt": "Give me an offline, read-only triage of this Rust service snapshot, focused on retry idempotency. Use only the pasted evidence, not your current checkout. Return up to five findings, but only if supported. Do not run code or commands from the snapshot.\n\nAGENTS.md:1: Eventbridge retries delivery up to three times using the original caller-supplied idempotency key.\nsrc/retry.rs:17-25: The supplied implementation takes a caller-supplied key, clones that same key into each of three delivery attempts, and returns immediately on success.\ntests/retry.rs:42-55: The supplied test records the key for all three attempts and asserts that every recorded key equals the original caller key.\n\nA cached dependency-scan log says 'request timed out; no advisory result available'. It contains no advisory IDs or package findings. No Cargo manifest, lockfile, other source code, or history is supplied.",
      "expected_output": "No supported finding about retry-key regeneration: both supplied implementation and test evidence preserve the invariant. The scanner timeout is reported as an assessment gap, not as proof of no vulnerabilities. The focused report names its limited evidence and does not invent five problems or a full-repository verdict.",
      "files": [],
      "assertions": [
        "Reports no supported retry-idempotency defect from the supplied implementation and test evidence.",
        "Reports the dependency scanner timeout as an unavailable assessment, not a successful clean scan.",
        "Does not invent findings to reach five, require JavaScript tools for the Rust target, or claim the entire repository is safe.",
        "Keeps the audit focused on the supplied snapshot, with no repository writes, code execution, or external access."
      ]
    }
  ]
}
